@model IEnumerable<PracticaBiblioteca.Models.AsignacionRol>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaAsignacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Persona)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rol)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaAsignacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Persona.IdPersona)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rol.IdRol)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAsignacionRol">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAsignacionRol">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAsignacionRol">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
